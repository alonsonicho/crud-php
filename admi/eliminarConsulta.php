<?php 

    // include('./db.php');
    
    // $codigo = $_GET['id_consulta'];

    // $sentencia = $conexion->prepare("DELETE consulta, cliente from consulta INNER JOIN cliente ON consulta.id_cliente=cliente.id_cliente WHERE consulta.id_consulta=?");
    // $resultado = $sentencia->execute([$codigo]);

    // if ($resultado === TRUE) {
    //     header('Location: consultas.php');
    // } else {
    //     header('Location: consultas.php');
    // }
?>